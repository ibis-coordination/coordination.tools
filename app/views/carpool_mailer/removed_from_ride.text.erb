Hi <%= @user.name %>,

<%= @driver_name %> can no longer give you a ride for <%= @carpool.name %>, so your seat was released.

We moved you back to the ride requests list so other drivers know you still need a seat. If you no longer need one, you can remove your request on the carpool page:

<%= @carpool_url %>
